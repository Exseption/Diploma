div.md-body-1(layout='column', style='background-color: white')
    div(layout='row')
        div(flex, style='margin-top: 10px')
            div(layout-margin)
                div
                    .md-title {{qc.question.title}}
                    | {{qc.question.person.name}} {{qc.question.person.surname}}
                    .md-caption {{qc.question.created | amUtc | amLocal | amDateFormat:'YYYY-MM-DD'}}
                    .md-body-1.ws-body(ng-bind-html="qc.question.body")
                div(layout='column')
                    .md-body-2
                        | Ответы
                    div(ng-repeat='ans in qc.question.answers')
                        div
                            .md-body-1
                                | {{$index+1}}. {{ans.body}}
                            .md-body-1
                                | Оценка {{ans.mark}}
                            div(layout-align='row')
                                md-button.md-icon-button
                                    md-icon(md-svg-icon='./img/ic_add_black_24px.svg')
                                md-button -
                            .md-caption(layout='row', layout-align='end') {{ans.created | amUtc | amLocal | amDateFormat:'YYYY-MM-DD'}} {{ans.person.name }} {{ans.person.surname}}
                div(layout='column')
                    .md-body-2
                        | Дать свой ответ
                    textarea(rows=10, ng-model='answer_text')
                    .md-body-1(layou='row')
                    div(layout='row', layout-align='end end')
                        md-button.md-raised(layout-align='end end', ng-click='qc.answer(answer_text, asnwer_files)') Дать ответ